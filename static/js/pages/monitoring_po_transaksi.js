function ValidateFileUploadDOC_PO() {
	var fuData = document.getElementById('DOC_PO');
	var FileUploadPath = fuData.value;
	if (FileUploadPath == '') {
		alert("Please upload an Document");
	} else {
		var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
		if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
		}
		else {
			document.getElementById("DOC_PO").value = "";
			alert("Only allows file types of DOC, DOCX and PDF. ");
			return false;
		}
	}
}

function ValidateFileUploadDOC_LC() {
	var fuData = document.getElementById('DOC_LC');
	var FileUploadPath = fuData.value;
	if (FileUploadPath == '') {
		alert("Please upload an Document");
	} else {
		var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
		if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
		}
		else {
			document.getElementById("DOC_LC").value = "";
			alert("Only allows file types of DOC, DOCX and PDF. ");
			return false;
		}
	}
}

function ValidateFileUploadAPPROVE_PEMENANG() {
	var fuData = document.getElementById('APPROVE_PEMENANG');
	var FileUploadPath = fuData.value;
	if (FileUploadPath == '') {
		alert("Please upload an Document");
	} else {
		var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
		if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
		}
		else {
			document.getElementById("APPROVE_PEMENANG").value = "";
			alert("Only allows file types of DOC, DOCX and PDF. ");
			return false;
		}
	}
}

function ValidateFileUploadDOC_OK() {
	var fuData = document.getElementById('DOC_OK');
	var FileUploadPath = fuData.value;
	if (FileUploadPath == '') {
		alert("Please upload an Document");
	} else {
		var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
		if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
		}
		else {
			document.getElementById("DOC_OK").value = "";
			alert("Only allows file types of DOC, DOCX and PDF. ");
			return false;
		}
	}
}

function ValidateFileUploadDOC_PPL() {
	var fuData = document.getElementById('DOC_PPL');
	var FileUploadPath = fuData.value;
	if (FileUploadPath == '') {
		alert("Please upload an Document");
	} else {
		var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
		if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
		}
		else {
			document.getElementById("DOC_PPL").value = "";
			alert("Only allows file types of DOC, DOCX and PDF. ");
			return false;
		}
	}
}


function ValidateFileUploadDOC_SHIPPING() {
	var fuData = document.getElementById('DOC_SHIPPING');
	var FileUploadPath = fuData.value;
	if (FileUploadPath == '') {
		alert("Please upload an Document");
	} else {
		var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
		if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
		}
		else {
			document.getElementById("DOC_SHIPPING").value = "";
			alert("Only allows file types of DOC, DOCX and PDF. ");
			return false;
		}
	}
}



function ValidateFileUploadDOC_PPL2() {
    var fuData = document.getElementById('DOC_PPL2');
    var FileUploadPath = fuData.value;
    if (FileUploadPath == '') {
        alert("Please upload an Document");
    } else {
        var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
        if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
        }
        else {
            document.getElementById("DOC_PPL2").value = "";
            alert("Only allows file types of DOC, DOCX and PDF. ");
            return false;
        }
    }
}

function ValidateFileUploadDOC_BILLING() {
    var fuData = document.getElementById('DOC_BILLING');
    var FileUploadPath = fuData.value;
    if (FileUploadPath == '') {
        alert("Please upload an Document");
    } else {
        var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
        if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
        }
        else {
            document.getElementById("DOC_BILLING").value = "";
            alert("Only allows file types of DOC, DOCX and PDF. ");
            return false;
        }
    }
}

function ValidateFileUploadDOC_BPN() {
    var fuData = document.getElementById('DOC_BPN');
    var FileUploadPath = fuData.value;
    if (FileUploadPath == '') {
        alert("Please upload an Document");
    } else {
        var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
        if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
        }
        else {
            document.getElementById("DOC_BPN").value = "";
            alert("Only allows file types of DOC, DOCX and PDF. ");
            return false;
        }
    }
}

function ValidateFileUploadDOC_MANIFEST() {
    var fuData = document.getElementById('DOC_MANIFEST');
    var FileUploadPath = fuData.value;
    if (FileUploadPath == '') {
        alert("Please upload an Document");
    } else {
        var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
        if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
        }
        else {
            document.getElementById("DOC_MANIFEST").value = "";
            alert("Only allows file types of DOC, DOCX and PDF. ");
            return false;
        }
    }
}

function ValidateFileUploadDOC_LS() {
    var fuData = document.getElementById('DOC_LS');
    var FileUploadPath = fuData.value;
    if (FileUploadPath == '') {
        alert("Please upload an Document");
    } else {
        var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
        if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
        }
        else {
            document.getElementById("DOC_LS").value = "";
            alert("Only allows file types of DOC, DOCX and PDF. ");
            return false;
        }
    }
}

function ValidateFileUploadDOC_INSURANCE() {
    var fuData = document.getElementById('DOC_INSURANCE');
    var FileUploadPath = fuData.value;
    if (FileUploadPath == '') {
        alert("Please upload an Document");
    } else {
        var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
        if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
        }
        else {
            document.getElementById("DOC_INSURANCE").value = "";
            alert("Only allows file types of DOC, DOCX and PDF. ");
            return false;
        }
    }
}

function ValidateFileUploadDOC_SPPB() {
    var fuData = document.getElementById('DOC_SPPB');
    var FileUploadPath = fuData.value;
    if (FileUploadPath == '') {
        alert("Please upload an Document");
    } else {
        var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
        if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
        }
        else {
            document.getElementById("DOC_SPPB").value = "";
            alert("Only allows file types of DOC, DOCX and PDF. ");
            return false;
        }
    }
}

function ValidateFileUploadDOC_SPTNP() {
    var fuData = document.getElementById('DOC_SPTNP');
    var FileUploadPath = fuData.value;
    if (FileUploadPath == '') {
        alert("Please upload an Document");
    } else {
        var Extension = FileUploadPath.substring(FileUploadPath.lastIndexOf('.') + 1).toLowerCase();
        if (Extension == "doc" || Extension == "docx" || Extension == "pdf") {
        }
        else {
            document.getElementById("DOC_SPTNP").value = "";
            alert("Only allows file types of DOC, DOCX and PDF. ");
            return false;
        }
    }
}

function loadData() {
	var e = document.getElementById("opco");
	var strUser = e.options[e.selectedIndex].value;

	// $.ajax({
	// 	url: $("#base-url").val() + 'Monitoring_po_import/getPoImportDB',
	// 	data: {
	// 		company: strUser
	// 	},
	// 	type: "post",
	// 	dataType: "html",
	// 	success: function (data) {
	// 		$('#example1').dataTable().fnClearTable();
	// 		$('#example1').dataTable().fnDestroy();

	// 		$('#resultPO').html('');
	// 		$('#resultPO').html(data);

	// 		var table = $('#example1').DataTable({
	// 			"columnDefs": [
	// 			{ "visible": false, "targets": 1 }
	// 			],
	// 			"order": [[ 1, 'asc' ]],
	// 			"displayLength": 25,
	// 			"drawCallback": function ( settings ) {
	// 				var api = this.api();
	// 				var rows = api.rows( {page:'current'} ).nodes();
	// 				var last=null;

	// 				api.column(1, {page:'current'} ).data().each( function ( group, i ) {
	// 					if ( last !== group ) {
	// 						$(rows).eq( i ).before(
	// 							'<tr class="group"><td colspan="9">'+group+'</td></tr>'
	// 							);

	// 						last = group;
	// 					}
	// 				} );
	// 			}
	// 		});

	// 		$('#example1 tbody').on( 'click', 'tr.group', function () {
	// 			var currentOrder = table.order()[0];
	// 			if ( currentOrder[0] === 1 && currentOrder[1] === 'asc' ) {
	// 				table.order( [ 1, 'desc' ] ).draw();
	// 			}
	// 			else {
	// 				table.order( [ 1, 'asc' ] ).draw();
	// 			}
	// 		});

	// 	},
	// 	error: function (xhr, status) {
	// 		alert("Sorry, there was a problem!");
	// 	},
	// 	complete: function (xhr, status) {
	// 	}
	// });


    mytable = $('#example1').DataTable({
        "bSort": false,
        "dom": 'rtip',
        "deferRender": true,
        
        "ajax": {
            url: $("#base-url").val() + 'Monitoring_po_import/getPoImportDB', 
            type: 'POST',
            data: {
                company: strUser,
            },
        },
        "columns":[
        {"data" : "TOMBOL1"},
        {"data" : "NO_PO"},
        {"data" : "NO_ITEM"},
        {"data" : "DESC"},
        {"data" : "VENDOR_NAME"},
        {"data" : "QTY"},
        {"data" : "NILAI_PO"},
        {"data" : "STAT_APPROVAL"},
        {"data" : "SATUAN"},
        {"data" : "TOD"},
        {"data" : "TOMBOL2"},
        ],
        "columnDefs": [
        { "visible": false, "targets": 1 }
        ],
        "order": [[ 1, 'asc' ]],
        "displayLength": 25,
        "drawCallback": function ( settings ) {
            var api = this.api();
            var rows = api.rows( {page:'current'} ).nodes();
            var last=null;

            api.column(1, {page:'current'} ).data().each( function ( group, i ) {
                if ( last !== group ) {
                    $(rows).eq( i ).before(
                        '<tr class="group"><td colspan="9">'+group+'</td></tr>'
                        );

                    last = group;
                }
            } );
        }
    });

    mytable.columns().every( function () {
        var that = this;

        $( 'input', this.header() ).on( 'keyup change', function () {
            if ( that.search() !== this.value ) {
                that.search( this.value ).draw();
            }
        });
    });

    $('#example1 tbody').on( 'click', 'tr.group', function () {
        var currentOrder = mytable.order()[0];
        if ( currentOrder[0] === 1 && currentOrder[1] === 'asc' ) {
            mytable.order( [ 1, 'desc' ] ).draw();
        }
        else {
            mytable.order( [ 1, 'asc' ] ).draw();
        }
    });
}

$(document).ready(function(){
	loadData();
	$('#DELIVERY_DATE').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
	$('#TGL_RELEASE').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
	$('#TGL_AMEND').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
	$('#TGL_NEGO').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
	$('#AKHIR_NEGO').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
	$('#TGL_BL').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
	$('#TGL_ETA').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');

    $('#TGL_BAYAR').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
    $('#TGL_BERLAKU').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
    $('#TGL_SPTNP').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
    $('#TGL_MANIFEST').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
    $('#TGL_TA').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
    
    $('#TGL_BERLAKU_LS').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
    $('#TGL_SPPB').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
    $('#TGL_CERT').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
    $('#TGL_PIB').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');
    $('#TGL_DO').mask("9999-99-99").attr('placeholder', 'yyyy-mm-dd (ex:1980-01-31)');


    // var table_job_list = $('#auction-list-table').DataTable({
    //     "ajax" : $("#base-url").val() + 'Auction/get_datatable_stat_null/',
    
    //     "columnDefs": [{
    //         "searchable": false,
    //         "orderable": false,
    //         "targets": 0
    //     }],

    //     "order": [[ 0, "desc" ]],
    
    //     "columns":[
    //     {"data" : null},
    //     {"data" : "PTM_SUBPRATENDER"},
    //     {"data"  :"PTM_PRATENDER"},
    //     {"data"  :"PTM_SUBJECT_OF_WORK"},
    //     {"data" : "PTM_REQUESTER_NAME"},
    //     {
    //         mRender : function(data,type,full){
    //             return '<td>Belum setup</td>';
    //         }},
    //         {
    //             mRender : function(data,type,full){
    //                 return '<a href="' + $("#base-url").val() + 'Auction/create/'+full.PTM_NUMBER+'" class="btn btn-default">Setup</a>'
    //             }},
    //             ],
    //         });

    // table_job_list.on( 'order.dt search.dt', function () {
    //     table_job_list.column(0, {search:'applied', order:'applied'}).nodes().each( function (cell, i) {
    //         cell.innerHTML = i+1;
    //     } );
    // } ).draw();

    $('body').on('click', '.btn-check', function()
    {
    	$("#transaksi_po").modal();

    	var e = document.getElementById("opco");
    	var strUser = e.options[e.selectedIndex].value;

    	$(".company").val(strUser);
        $('#example').dataTable().fnClearTable();
        $('#example').dataTable().fnDestroy();
    	// $.ajax({
    	// 	url: $("#base-url").val() + 'Monitoring_po_import/getPoImportSap',
    	// 	data: {
    	// 		company: strUser
    	// 	},
    	// 	type: "post",
    	// 	dataType: "html",
    	// 	success: function (data) {
    	// 		$('#example').dataTable().fnClearTable();
    	// 		$('#example').dataTable().fnDestroy();

     //            // var result = $('<div />').append(data).find('#resultHargaAwal').html();
     //            $('#resultPOSap').html('');
     //            $('#resultPOSap').html(data);
     //            //$('#example').DataTable();

     //            var table = $('#example').DataTable({
     //            	"columnDefs": [
     //            	{ "visible": false, "targets": 1 }
     //            	],
     //            	"order": [[ 1, 'asc' ]],
     //            	"displayLength": 25,
     //            	"drawCallback": function ( settings ) {
     //            		var api = this.api();
     //            		var rows = api.rows( {page:'current'} ).nodes();
     //            		var last=null;

     //            		api.column(1, {page:'current'} ).data().each( function ( group, i ) {
     //            			if ( last !== group ) {
     //            				$(rows).eq( i ).before(
     //            					'<tr class="group"><td colspan="9">'+group+'</td></tr>'
     //            					);

     //            				last = group;
     //            			}
     //            		} );
     //            	}
     //            } );

     //            $('#example tbody').on( 'click', 'tr.group', function () {
     //            	var currentOrder = table.order()[0];
     //            	if ( currentOrder[0] === 1 && currentOrder[1] === 'asc' ) {
     //            		table.order( [ 1, 'desc' ] ).draw();
     //            	}
     //            	else {
     //            		table.order( [ 1, 'asc' ] ).draw();
     //            	}
     //            } );

     //        },
     //        error: function (xhr, status) {
     //        	alert("Sorry, there was a problem!");
     //        },
     //        complete: function (xhr, status) {
     //            //$('#showresults').slideDown('slow')
     //        }
     //    });

        mytable = $('#example').DataTable({
            "bSort": false,
            "dom": 'rtip',
            "deferRender": true,

            "ajax": {
                url: $("#base-url").val() + 'Monitoring_po_import/getPoImportSap', 
                type: 'POST',
                data: {
                    company: strUser,
                },
            },
            "columns":[
            {"data" : "TOMBOL1"},
            {"data" : "NO_PO"},
            {"data" : "NO_ITEM"},
            {"data" : "DESC"},
            {"data" : "VENDOR_NAME"},
            {"data" : "QTY"},
            {"data" : "NILAI_PO"},
            {"data" : "STAT_APPROVAL"},
            {"data" : "SATUAN"},
            {"data" : "TOD"},
            ],
            "columnDefs": [
            { "visible": false, "targets": 1 }
            ],
            "order": [[ 1, 'asc' ]],
            "displayLength": 25,
            "drawCallback": function ( settings ) {
                var api = this.api();
                var rows = api.rows( {page:'current'} ).nodes();
                var last=null;

                api.column(1, {page:'current'} ).data().each( function ( group, i ) {
                    if ( last !== group ) {
                        $(rows).eq( i ).before(
                            '<tr class="group"><td colspan="9">'+group+'</td></tr>'
                            );

                        last = group;
                    }
                } );
            }
        });

        mytable.columns().every( function () {
            var that = this;

            $( 'input', this.header() ).on( 'keyup change', function () {
                if ( that.search() !== this.value ) {
                    that.search( this.value ).draw();
                }
            });
        });

        $('#example tbody').on( 'click', 'tr.group', function () {
            var currentOrder = mytable.order()[0];
            if ( currentOrder[0] === 1 && currentOrder[1] === 'asc' ) {
                mytable.order( [ 1, 'desc' ] ).draw();
            }
            else {
                mytable.order( [ 1, 'asc' ] ).draw();
            }
        });

    })

$('body').on('click', '.btn-update', function()
{
   $.ajax({
      url: $("#base-url").val() + 'Monitoring_po_import/updatePoImportSap',
      type: "post",
      dataType: "json",
      success: function (data) {
      },
      error: function (xhr, status) {
         alert("Sorry, there was a problem!");
     },
     complete: function (xhr, status) {
     }
 });

})

$('body').on('click', '.update-detail', function()
{
    $('#formrooms').attr('action',$("#base-url").val() + 'Monitoring_po_import/updateDetailPo')
    $('#formrooms').submit();  
})

$('body').on('click', '.save-detail', function()
{
    $('#formrooms2').attr('action',$("#base-url").val() + 'Monitoring_po_import/saveDetailPo')
    $('#formrooms2').submit();  
})

$('body').on('click', '.save-custom', function()
{
   $('#formrooms3').attr('action',$("#base-url").val() + 'Monitoring_po_import/saveCustom')
   $('#formrooms3').submit();  
})

$('body').on('click', '.btn-edit', function()
{
    $("#edit_transaksi_po").modal();
    var no_po = $(this).attr("no_po");
    $(".NO_PO").val(no_po);

    $.ajax({
        url: $("#base-url").val() + 'Monitoring_po_import/getListDocShip',
        data: {
            no_po: no_po
        },
        type: "post",
        dataType: "html",
        success: function (data) {
            $('#example2').dataTable().fnClearTable();
            $('#example2').dataTable().fnDestroy();

            $('#resultDocShip').html('');
            $('#resultDocShip').html(data);

            var table = $('#example2').DataTable();
        },
        error: function (xhr, status) {
            alert("Sorry, there was a problem!");
        },
        complete: function (xhr, status) {
        }
    });

    $.ajax({
        url: $("#base-url").val() + 'Monitoring_po_import/getListCustom',
        data: {
            no_po: no_po
        },
        type: "post",
        dataType: "html",
        success: function (data) {
            $('#example3').dataTable().fnClearTable();
            $('#example3').dataTable().fnDestroy();

            $('#resultCustom').html('');
            $('#resultCustom').html(data);

            var table = $('#example3').DataTable();
        },
        error: function (xhr, status) {
            alert("Sorry, there was a problem!");
        },
        complete: function (xhr, status) {
        }
    });

    $.ajax({
        url: $("#base-url").val() + 'Monitoring_po_import/detailPo',
        data: {
            no_po: no_po
        },
        type: "post",
        dataType: "json",
        success: function (data) {
            $("#NO_LC").val(data.NO_LC);
            $("#LSD").val(data.LSD);
            $("#EXPIRY").val(data.EXPIRY);
            $("#FORWARDER").val(data.FORWARDER);
            $("#KET").val(data.KET);
            $("#TOP").val(data.TOP);
            $("#TOD").val(data.TOD);
            $("#NILAI").val(data.NEW_NILAI);
            $("#DELIVERY_DATE").val(data.NEW_DELIVERY_DATE);
            $("#TGL_RELEASE").val(data.NEW_TGL_RELEASE);
            $("#TGL_AMEND").val(data.NEW_TGL_AMEND);
            $("#TGL_NEGO").val(data.NEW_TGL_NEGO);
            $("#AKHIR_NEGO").val(data.NEW_AKHIR_NEGO);

        },
        error: function (xhr, status) {
            alert("Sorry, there was a problem!");
        },
        complete: function (xhr, status) {
        }
    });

});

$('body').on('click', '.btn-edit-doc-ship', function()
{
    var id = $(this).attr("id");
    $(".ID").val(id);

    $.ajax({
        url: $("#base-url").val() + 'Monitoring_po_import/detailDocShip',
        data: {
            id: id
        },
        type: "post",
        dataType: "json",
        success: function (data) {
            $(".ID").val(id);
            $("#TGL_BL").val(data.NEW_TGL_BL);
            $("#TGL_ETA").val(data.NEW_TGL_ETA);
            $("#NILAI_INV").val(data.NEW_NILAI_INV);
            $("#JML_CONTAINER").val(data.NEW_JML_CONTAINER);
            $("#KUANTITAS_BL").val(data.NEW_KUANTITAS_BL);
            $("#NO_BL").val(data.DOC_SHIP.NO_BL);
            $("#NAMA_ANGKUT").val(data.DOC_SHIP.NAMA_ANGKUT);
            $("#POL").val(data.DOC_SHIP.POL);
            $("#POD").val(data.DOC_SHIP.POD);
            $("#NEGARA_ASAL").val(data.DOC_SHIP.NEGARA_ASAL);
            $("#NO_PPL_BARANG").val(data.DOC_SHIP.NO_PPL_BARANG);
            $("#JENIS_KEMASAN").val(data.DOC_SHIP.JENIS_KEMASAN);
            $("#SATUAN").val(data.DOC_SHIP.SATUAN);
        },
        error: function (xhr, status) {
            alert("Sorry, there was a problem!");
        },
        complete: function (xhr, status) {
        }
    });

});

$('body').on('click', '.btn-edit-custom', function()
{
    var id = $(this).attr("id");
    $(".IDC").val(id);

    $.ajax({
        url: $("#base-url").val() + 'Monitoring_po_import/detailCustom',
        data: {
            id: id
        },
        type: "post",
        dataType: "json",
        success: function (data) {
            $(".IDC").val(id);

            $("#TGL_BAYAR").val(data.NEW_TGL_BAYAR);
            $("#TGL_BERLAKU").val(data.NEW_TGL_BERLAKU);
            $("#TGL_SPTNP").val(data.NEW_TGL_SPTNP);
            $("#TGL_MANIFEST").val(data.NEW_TGL_MANIFEST);
            $("#TGL_TA").val(data.NEW_TGL_TA);

            $("#TGL_BERLAKU_LS").val(data.NEW_TGL_BERLAKU_LS);
            $("#TGL_SPPB").val(data.NEW_TGL_SPPB);
            $("#TGL_CERT").val(data.NEW_TGL_CERT);
            $("#TGL_PIB").val(data.NEW_TGL_PIB);
            $("#TGL_DO").val(data.NEW_TGL_DO);

            $("#OTHER").val(data.NEW_OTHER);
            $("#KUOTA_LARTAS").val(data.NEW_KUOTA_LARTAS);
            $("#SISA_KUOTA").val(data.NEW_SISA_KUOTA);
            $("#NILAI_LS").val(data.NEW_NILAI_LS);

            $("#HS_CODE").val(data.DOC_CUSTOM.HS_CODE);
            $("#NO_PPL_BM").val(data.DOC_CUSTOM.NO_PPL_BM);
            $("#NO_PPL_PDRI").val(data.DOC_CUSTOM.NO_PPL_PDRI);
            $("#NO_PI_IP").val(data.DOC_CUSTOM.NO_PI_IP);
            $("#NO_SPTNP").val(data.DOC_CUSTOM.NO_SPTNP);
            $("#NO_LS").val(data.DOC_CUSTOM.NO_LS);
            $("#NO_CERT").val(data.DOC_CUSTOM.NO_CERT);
            $("#NO_PIB").val(data.DOC_CUSTOM.NO_PIB);
            $("#OTHER").val(data.DOC_CUSTOM.OTHER);
        },
        error: function (xhr, status) {
            alert("Sorry, there was a problem!");
        },
        complete: function (xhr, status) {
        }
    });

});

$('body').on('click', '.btn-delete-doc-ship', function()
{
    var id = $(this).attr("id");
    $(".ID").val(id);

    $.ajax({
        url: $("#base-url").val() + 'Monitoring_po_import/deleteDetailPo',
        data: {
            id: id
        },
        type: "post",
        dataType: "json",
        success: function (data) {
            location.reload(); 
        },
        error: function (xhr, status) {
            alert("Sorry, there was a problem!");
        },
        complete: function (xhr, status) {
        }
    });

});

$('body').on('click', '.btn-delete-custom', function()
{
    var id = $(this).attr("id");
    $(".ID").val(id);

    $.ajax({
        url: $("#base-url").val() + 'Monitoring_po_import/deleteCustom',
        data: {
            id: id
        },
        type: "post",
        dataType: "json",
        success: function (data) {
            location.reload(); 
        },
        error: function (xhr, status) {
            alert("Sorry, there was a problem!");
        },
        complete: function (xhr, status) {
        }
    });

});
})